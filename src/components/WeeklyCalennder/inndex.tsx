import React from "react";

export interface WeeklyCalenderProps {
  children?: React.ReactNode;
}

const WeeklyCalender = ({}: WeeklyCalenderProps) => {
  return <div>WeeklyCalender</div>;
};

export default WeeklyCalender;
